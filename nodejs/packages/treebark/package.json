{
  "name": "treebark",
  "version": "1.0.0",
  "description": "Safe HTML tree structures for Markdown and content-driven apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "build:browser": "tsc -p tsconfig.browser.json && node scripts/create-browser-bundle.js",
    "build:vite": "vite build",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "markdown",
    "template",
    "safe",
    "html",
    "tree",
    "schema"
  ],
  "author": "Dan Marshall",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-dts": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}