{
  "name": "treebark",
  "version": "2.0.7",
  "description": "Safe HTML tree structures for Markdown and content-driven apps",
  "type": "module",
  "files": [
    "dist",
    "browser"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./string": {
      "types": "./dist/string.d.ts",
      "default": "./dist/string.js"
    },
    "./dom": {
      "types": "./dist/dom.d.ts",
      "default": "./dist/dom.js"
    }
  },
  "scripts": {
    "build": "tsc && npm run build:browser && npm run build:browser:min && npm run copy:browser",
    "build:browser": "vite build --config vite.browser.config.ts",
    "build:browser:min": "vite build --config vite.browser.min.config.ts",
    "build:vite": "vite build",
    "copy:browser": "node -e \"const fs=require('fs'),path=require('path'); const dir=path.join(__dirname,'../../../docs/assets'); if(!fs.existsSync(dir)) fs.mkdirSync(dir,{recursive:true}); fs.copyFileSync('browser/treebark-browser.js', path.join(dir,'treebark-browser.js')); fs.copyFileSync('browser/treebark-browser.min.js', path.join(dir,'treebark-browser.min.js'));\"",
    "clean": "rm -rf dist browser",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "markdown",
    "template",
    "safe",
    "html",
    "tree",
    "schema"
  ],
  "author": "Dan Marshall",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
