{
  "name": "treebark-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/treebark && npm run build --workspace=packages/markdown-it-treebark && npm run build --workspace=packages/playground && npm run build --workspace=packages/markdown-playground && npm run build --workspace=packages/test",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "cross-env": "^7.0.3",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.1.9"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "jsdom": "^26.1.0"
  }
}
