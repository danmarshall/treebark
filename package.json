{
  "name": "treebark-docs",
  "version": "1.0.0",
  "description": "Documentation generation for Treebark",
  "private": true,
  "scripts": {
    "docs:generate": "node generate-docs.js",
    "docs:build": "npm run docs:generate && cd docs && bundle exec jekyll build",
    "docs:serve": "npm run docs:generate && cd docs && bundle exec jekyll serve"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}